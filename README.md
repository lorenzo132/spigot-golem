# spigot-golem
A simple plugin for Spigot to improve copper golems


Build with Maven: `mvn clean package`
Place the generated JAR from `target/` into your server's `plugins/` folder
Restart your server
Configure settings in `plugins/CopperGolemSorter/config.yml`
Use `/coppergolem reload` to apply configuration changes
